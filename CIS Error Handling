def acres_to_hecaters():
    try:
        acres=float(input("Enter to acres:"))
        if acres<0:
            print("Negative values are not allowed.Please try agian.")
        else:
            conversion_factor=0.404686
            hecaters=acres*conversion_factor
            print(acres,"converts to",hecaters)
    except ValueError:
        print("invalid input.Please enter a numerical value.")


def quarts_to_liters():
    """
Converts a given volume in US liquid quarts to liters.
1 US quart=0.946353 liters
"""
    try:
        quarts=float(input("Enter to quarts"))
        if quarts<0:
            print("Negative values are not allowed. Please try agian.")
        else:
            conversion_factor=0.946353
            liters=quarts*0.946353
            print(quarts,"converts to",liters)
    except ValueError:
        print("Invaild input.Please enter a numerical value.")


def fahrenheit_to_kelivn():

    try:
        fahrenheit=float(input("Enter to Fahrenheit "))
        temp_k=(fahrenheit-32)*(5/9)+273.15
        print(fahrenheit,"converts to", temp_k)
    except ValueError:
        print("Invalid input.Please a numerical value")
    

    print("progam running")


    acres_to_hecaters()
    quarts_to_liters()
    fahrenheit_to_kelivn()
    user_input=input("Enter n to quit or y to run agian:")
    if user_input.lower()=='n':
        break
print("progam ended.")
